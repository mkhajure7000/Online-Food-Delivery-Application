<h2>Cart show</h2>
<ul>
  <% @cart.cart_items.each do |cart_item| %>
    <li>Item: <%= cart_item.food.name %>/ Price: <%= number_to_currency(cart_item.food.price) %>/ Quantity: <%= cart_item.quantity %>/ Total Price: <%= number_to_currency(cart_item.total_price) %>
    <ul>
      <li>
        <%= link_to "Add one", cart_item_add_path(:id => line_item), method: :post %>
      </li>
    </ul>
  <% end %>
</ul>
